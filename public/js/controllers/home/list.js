'use strict';

(function (app) {
    app.controller('Home.ListController', [
        '$scope',
        '$state',
        '$http',
        function ($scope, $state, $http) {

        }
    ]);
}(window.MTG.main));
