'use strict';

(function (app) {
    app.controller('Home.OrderController', [
        '$scope',
        '$state',
        '$stateParams',
        '$http',
        function ($scope, $state, $stateParams, $http) {

        }
    ]);
}(window.MTG.main));
